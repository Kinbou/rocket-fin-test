services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: rocketfin
      POSTGRES_PASSWORD: rocketfin
      POSTGRES_DB: rocketfin
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: